/*export function ArraysExercise(): number[] {
  const a: boolean = true;
  const b: number = 234;
  const c: string = `Text - ${b} wiehfowiehf`;

  const arr: number[] = [5, 7, 20, 12, 54, 23, 65, 43, 23, 24, 11, 25];

  let val: number = arr[4];
  val = b;

  val = 540;
  arr[4] = val;

  arr[11] = 100;

  return arr;
}



/**
 * Create array of 4 numbers.
 *
 * Return their sum.
 */
export function NumArraySum(): string {
const a: number[] = [22, 13, 54, 19];
const b: number = a[0] + a[1] + a[2] + a[3];
const c: string = `Suma niza 22, 13, 54, 19 iznosi ${b} `;


return c;
}

/**
 * Create array of N boolean values.
 *
 * Return first value.
 */
export function BooleanArrayFirst(): string {

const a: boolean[] = [true, false, false, false, true, false, true, true];

const b:boolean = a[0];
const c:string = `Prvi boolean je ${b}`

  return c;
}

/**
 * Create array of 6 string values.
 *
 * Return concatenated string of all even elements.
 */
export function StringArrayEvenElements(): string {
const a:string[] = ["pas ","macka ","je ","je ","los","cool"];
const b:string =a[1] + a[3] + a[5];

  return b;
}

/**
 * Create array of 6 string values.
 *
 * Return concatenated string of all odd elements.
 */
export function StringArrayOddElements(): string {
const a:string[] = ["pas ","macka ","je ","je ","los","cool"];
const b:string =a[0] + a[2] + a[4];

  return b;
}
